{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install && echo -e 'a\\ny\\n' | pnpm approve-builds",
  "framework": "nextjs",
  "regions": ["gru1"],
  "git": {
    "deploymentEnabled": true
  }
}
