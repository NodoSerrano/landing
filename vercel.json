{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install && (echo 'a'; echo ''; yes) | pnpm approve-builds",
  "framework": "nextjs",
  "regions": ["gru1"],
  "git": {
    "deploymentEnabled": true
  }
}
